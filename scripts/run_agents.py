# DEPRECATED
# 이 스크립트는 Anthropic API를 직접 호출하는 방식이었어요.
# 현재 아키텍처(Claude Max 로컬 실행)에서는 사용하지 않습니다.
#
# 대신 Mac 터미널에서 이 리포 안에 들어간 뒤 `claude` 실행하면 돼요.
# Claude(저)가 skill/ 폴더와 CLAUDE.md를 읽고 직접 글을 써서
# _posts/ 에 저장해 줍니다.
#
# 나중에 자동화를 붙이고 싶으면 이 파일을 참고용으로 남겨뒀어요.
